"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

import gm_lcm.LcmNumpyArray

class LcmTrajectoryData(object):
    __slots__ = ["timestamp", "trajectory", "current_speed"]

    def __init__(self):
        self.timestamp = 0
        self.trajectory = gm_lcm.LcmNumpyArray()
        self.current_speed = 0.0

    def encode(self):
        buf = BytesIO()
        buf.write(LcmTrajectoryData._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">q", self.timestamp))
        assert self.trajectory._get_packed_fingerprint() == gm_lcm.LcmNumpyArray._get_packed_fingerprint()
        self.trajectory._encode_one(buf)
        buf.write(struct.pack(">d", self.current_speed))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != LcmTrajectoryData._get_packed_fingerprint():
            raise ValueError("Decode error")
        return LcmTrajectoryData._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = LcmTrajectoryData()
        self.timestamp = struct.unpack(">q", buf.read(8))[0]
        self.trajectory = gm_lcm.LcmNumpyArray._decode_one(buf)
        self.current_speed = struct.unpack(">d", buf.read(8))[0]
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if LcmTrajectoryData in parents: return 0
        newparents = parents + [LcmTrajectoryData]
        tmphash = (0x62c8c7c47cae6be1+ gm_lcm.LcmNumpyArray._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if LcmTrajectoryData._packed_fingerprint is None:
            LcmTrajectoryData._packed_fingerprint = struct.pack(">Q", LcmTrajectoryData._get_hash_recursive([]))
        return LcmTrajectoryData._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

